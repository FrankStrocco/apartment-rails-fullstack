<p id="notice"><%= notice %></p>

<h2 style="color: blue">Welcome! You are signed is as <%= current_user.email%></h2>

<%= link_to "Sign Out", destroy_user_session_path, method: :delete %>
<!-- 🙄 -->

<br/>

<h1>Apartments</h1>

<br />

<form action="address_search" method="get">
    <input type="text" name="address_search" placeholder="Search by Address">
    <input type="submit" name="" value="Search">
    </form>

<!-- <form class="" action="fuzzy_search" method="get">
    <input type="text" name="fuzzy_address_search" placeholder="Search by Address">
    <input type="submit" name="" value="Search fuzzy">
</form> -->
<br />

<table>
  <thead>
    <tr>
      <th>Address</th>
      <th>City</th>
      <th>State</th>
      <th>Zip code</th>
      <th>Building manager</th>
      <th>Phone</th>
      <th>Hours</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @apartments.each do |apartment| %>
      <tr>
        <td><%= apartment.address %></td>
        <td><%= apartment.city %></td>
        <td><%= apartment.state %></td>
        <td><%= apartment.zip_code %></td>
        <td><%= apartment.building_manager %></td>
        <td><%= apartment.phone %></td>
        <td><%= apartment.hours %></td>
        <td><%= link_to 'Show', apartment %></td>
        <td><%= link_to 'Edit', edit_apartment_path(apartment) %></td>
        <td><%= link_to 'Destroy', apartment, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Apartment', new_apartment_path %>
